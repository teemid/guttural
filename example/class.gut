from io import print


class Greeter(Object)
    var name

    function constructor (name)
        self.name = name
    end

    function greet ()
        print('Hello, {name}'.format(name=name))
    end
end


greeter = Greeter('Emil')
greeter.greet()
